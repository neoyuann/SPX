requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
python-dateutil>=2.9
# Only needed to run import_excel_companies.py (bulk-importing a roster
# spreadsheet) — the tracker itself never imports this.
openpyxl>=3.1
